# ═══════════════════════════════════════════════════════════════
# TokenGraveyard v2.0 - Environment Configuration
# ═══════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────
# Network Configuration
# ───────────────────────────────────────────────────────────────
# ENVIRONMENT: Network to use (TEST for testnet, MAIN for mainnet)
ENVIRONMENT=TEST

# ───────────────────────────────────────────────────────────────
# Account Configuration
# ───────────────────────────────────────────────────────────────
# Your Hedera account ID (format: 0.0.xxxxx)
ACCOUNT_ID=

# Your private key (DER-encoded hex string)
PRIVATE_KEY=

# ───────────────────────────────────────────────────────────────
# Ecosystem Contracts (Required for Deployment)
# ───────────────────────────────────────────────────────────────
# $LAZY Token Contract ID
# The ERC20-compatible token used for payments
LAZY_TOKEN=0.0.48486075

# LazyGasStation Contract ID
# Centralized payment handler with burn mechanism
LAZY_GAS_STATION_CONTRACT_ID=

# LazyDelegateRegistry Contract ID (Optional)
# Leave empty or use 0.0.0 if not using delegation
LAZY_DELEGATE_REGISTRY_CONTRACT_ID=

# ───────────────────────────────────────────────────────────────
# TokenGraveyard Contract (Set After Deployment)
# ───────────────────────────────────────────────────────────────
# TokenGraveyard Contract ID
# Will be populated after running: npm run deploy
GRAVEYARD_CONTRACT_ID=

# ───────────────────────────────────────────────────────────────
# Script Configuration
# ───────────────────────────────────────────────────────────────
# Contract name for ABI loading (usually TokenGraveyard)
CONTRACT_NAME=TokenGraveyard

# Event name for log monitoring (for getContractLogs.js)
# Options: NFTsBuried, TokenAssociated, RoleUpdated, CostUpdated
EVENT_NAME=NFTsBuried

# ───────────────────────────────────────────────────────────────
# Deployment Configuration (Optional - Will prompt if not set)
# ───────────────────────────────────────────────────────────────
# Initial cost in $LAZY tokens (with decimals)
# Example: For 100 LAZY with 8 decimals = 10000000000
INITIAL_LAZY_COST=

# Initial burn percentage (0-100)
# Percentage of payment that gets burned
INITIAL_LAZY_BURN_PERCENTAGE=50

# ───────────────────────────────────────────────────────────────
# Legacy Variables (kept for backwards compatibility)
# ───────────────────────────────────────────────────────────────
CONTRACT_ID=
LAZY_CONTRACT=0.0.48486072
LAZY_BURN_PERC=50

# ───────────────────────────────────────────────────────────────
# Quick Start Guide
# ───────────────────────────────────────────────────────────────
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Fill in your ACCOUNT_ID and PRIVATE_KEY
#
# 3. Fill in ecosystem contract addresses:
#    - LAZY_TOKEN (already set for testnet)
#    - LAZY_GAS_STATION_CONTRACT_ID
#    - LAZY_DELEGATE_REGISTRY (or leave empty)
#
# 4. Deploy the contract:
#    npm run deploy
#
# 5. Copy the deployed contract ID to GRAVEYARD_CONTRACT_ID
#
# 6. Start interacting:
#    npm run interact
#
# ───────────────────────────────────────────────────────────────
# Security Warnings
# ───────────────────────────────────────────────────────────────
#
# ⚠️  NEVER commit your .env file to version control!
# ⚠️  NEVER share your PRIVATE_KEY with anyone!
# ⚠️  Keep your .env file secure and backed up safely
#
# ───────────────────────────────────────────────────────────────
